# CLAUDE.md

**Read [AGENTS.md](AGENTS.md) first — before any source file, before any spec.**

It is the authoritative build-agent guidance for this repository: the document
hierarchy, spec-first discipline, the async boundary, the two-tier agent
architecture, PROTECTED_PATHS, build phases, and security invariants. Everything
an agent needs to work in this repo correctly starts there.

If you are a runtime swarm agent governed by `agents/*.md`, those contract files
are your instructions instead — see AGENTS.md §1.
